name: Vercel Preview

# Build and deploy a Vercel Preview for every PR to main, then comment the URL
# so changes are reviewable on a live deployment before merge. Production still
# ships from main via the manual `vercel --prod` flow — this only adds previews.
#
# The Vercel org/project IDs are identifiers (not secrets) and are inlined so the
# only required setup is a single repo secret: VERCEL_TOKEN (a Vercel access
# token with access to the agrilogys-projects team).

on:
  pull_request:
    branches: [main]

# One in-flight preview per PR; newer pushes cancel the older build.
concurrency:
  group: vercel-preview-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

env:
  VERCEL_ORG_ID: team_0qvi19Uq395tQgY3GWshb0YV
  VERCEL_PROJECT_ID: prj_qiqO8LWnoYfgEGxkyALrQ6AUw3oz

jobs:
  deploy-preview:
    name: Deploy Preview
    runs-on: ubuntu-latest
    # Skip cleanly (rather than fail) if the token secret hasn't been set yet.
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Pull Vercel environment (preview)
        run: vercel pull --yes --environment=preview --token="$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Build project artifacts
        run: vercel build --token="$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Deploy prebuilt artifacts to Vercel (preview)
        id: deploy
        run: |
          url="$(vercel deploy --prebuilt --token="$VERCEL_TOKEN")"
          echo "url=$url" >> "$GITHUB_OUTPUT"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Comment preview URL on the PR
        uses: actions/github-script@v7
        with:
          script: |
            const url = "${{ steps.deploy.outputs.url }}";
            const marker = "<!-- vercel-preview -->";
            const body = `${marker}\n### ▲ Vercel Preview\n\nThis PR is deployed to a live preview:\n\n**${url}**\n\n_Updated on every push to this PR._`;
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            const existing = comments.find((c) => c.body && c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
